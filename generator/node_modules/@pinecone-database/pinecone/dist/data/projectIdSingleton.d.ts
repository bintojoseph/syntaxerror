import type { PineconeConfiguration } from './types';
export declare const ProjectIdSingleton: {
    getProjectId: (config: PineconeConfiguration) => Promise<any>;
    _reset: () => void;
    _set: (config: any, projectId: any) => void;
};
